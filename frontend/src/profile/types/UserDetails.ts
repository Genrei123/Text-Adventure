interface UserDetails {
    id: number;
    username: string;
    email: string;
    private: boolean;
    model: string;
    admin: boolean;
    emailVerified: boolean;
    totalCoins: number;
    createdAt: string;
    updatedAt: string;

    // ADD BIO
    // ADD PROFILE PICTURE
    // ADD GAMES CREATED
    // ADD WHERE USER COMMENTED
  }